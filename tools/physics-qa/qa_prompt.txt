You are a physics-QA reviewer for a 2D soft-body character in a top-down action game.
You are shown 3 screenshots taken 0.6s apart from the same live simulation.

The character is a humanoid built from a particle skeleton plus soft-body colliders
(limbs, head, torso), solved with an XPBD constraint system. In a healthy run the
silhouette stays recognisably humanoid: limbs keep their proportions, the body holds
roughly its original scale, and the figure does not leave the arena.

Judge ONLY these failure modes:
1. EXPLOSION - the character is blown out of its humanoid shape entirely. This is
   the XPBD divergence failure: constraint compliance or substep count is wrong, so
   constraint error compounds every tick instead of being corrected. What you see:
   body parts scattered far from one another, limbs stretched into long thin spikes
   or rubber bands, the skeleton detached from its colliders, or the figure inflated
   to many times its normal size and no longer readable as a body. The parts are
   still drawn - they have simply lost the constraints that held them together.
   Be strict here: a run where you cannot tell it is a humanoid is an EXPLOSION,
   even if every part is still on screen.
2. WOBBLE  - the character keeps its humanoid shape but will not settle: violent
   jitter, limbs flailing through each other, silhouette thrashing between frames,
   or slow oscillation that never damps out.
3. COLLAPSE - the body loses volume and melts into a blob: limbs clump together at
   the centre, the figure sags into a heap, or it sinks through the ground.
4. OK      - the figure stays humanoid and stable across all three frames.

Notes:
- This is a top-down view, so the figure is drawn as an outlined body with a gun;
  parts overlapping the environment is normal and NOT a failure.
- Transient bright flashes, expanding red circles, or bursts of small dots are
  particle VFX, not the character. Ignore them entirely when judging the body; do
  not read them as damage, explosion, or blood. Judge only the character silhouette.
- Compare the frames to each other. A single odd frame matters less than a body that
  keeps degrading across all three.

Answer with ONLY a JSON object, no markdown fence, in exactly this shape:
{
  "verdict": "OK" | "EXPLOSION" | "WOBBLE" | "COLLAPSE",
  "confidence": 0.0-1.0,
  "severity": 0-10,
  "observations": "one or two sentences on what you actually see across the frames",
  "suspect_params": ["names of physics parameters most likely responsible, if not OK"],
  "suggested_direction": "which way to move those parameters, if not OK"
}
